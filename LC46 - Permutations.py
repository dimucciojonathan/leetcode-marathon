class Solution:
    def p(self, current, nums):
        # Edge Cases
        if len(nums) == 1:
            return [nums]
        elif len(nums) == 2: # Only 2 permutations

        
        return

    def permute(self, nums) -> list:
        sol = []
        for i in nums:
            p(i, nums.remove(i))


